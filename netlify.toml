[build]
  command = "pnpm run build"
  publish = "dist"
  ignore = "git diff --name-only $CACHED_COMMIT_REF $COMMIT_REF | grep -qvE '^src/content/' && exit 1 || exit 0"

[build.environment]
  NODE_VERSION = "22"
  SECRETS_SCAN_OMIT_KEYS = "TINA_BRANCH"
